repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--allow-multiple-documents]
        exclude: ^deployments/helm/mcp-server-langgraph/templates/
      - id: check-added-large-files
        args: ['--maxkb=500']
        exclude: ^uv\.lock$  # uv lockfile can be large (738KB)
      - id: check-merge-conflict
      - id: detect-private-key
      - id: check-json
      - id: check-toml
      - id: mixed-line-ending

  - repo: https://github.com/psf/black
    rev: 25.9.0
    hooks:
      - id: black
        args: [--line-length=127]
        language_version: python3.11

  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: [--profile=black, --line-length=127]
        # NOTE: Using 5.13.2 for stability. Version 7.0.0+ in requirements-test.txt
        # is for local development. Both versions are compatible with same config.

  - repo: https://github.com/pycqa/flake8
    rev: 7.3.0
    hooks:
      - id: flake8
        args: ["--max-line-length=127", "--extend-ignore=E203,W503"]
        additional_dependencies: [flake8-docstrings]

  - repo: https://github.com/pycqa/bandit
    rev: 1.8.6
    hooks:
      - id: bandit
        args: [-ll, -x, tests]

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.28.0
    hooks:
      - id: gitleaks

  # Mypy disabled in pre-commit due to 500+ type annotation issues
  # Run manually with: mypy --ignore-missing-imports src/
  # TODO: Re-enable after gradual type annotation improvements
  #- repo: https://github.com/pre-commit/mirrors-mypy
  #  rev: v1.18.2
  #  hooks:
  #    - id: mypy
  #      args: [
  #        --ignore-missing-imports,
  #        --warn-unused-configs,
  #        --show-error-codes,
  #        --pretty,
  #        --no-error-summary
  #      ]
  #      exclude: ^(deployments/langgraph-platform/|tests/|scripts/|examples/)
  #      additional_dependencies: [
  #        types-PyYAML,
  #        types-redis,
  #        types-requests
  #      ]

  # GitHub Actions workflow validation
  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.29.4
    hooks:
      - id: check-github-workflows
        name: Validate GitHub Actions workflows
        description: Validates GitHub Actions workflow syntax
        files: ^\.github/workflows/.*\.ya?ml$
