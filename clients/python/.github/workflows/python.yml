# ==============================================================================
# Python Client Package - CI Pipeline
# ==============================================================================
#
# NOTE: This file is auto generated by OpenAPI Generator.
# URL: https://openapi-generator.tech
#
# Purpose:
#   - Run tests for the auto-generated Python client
#   - Ensure compatibility across Python 3.9-3.13
#   - Validate client code against OpenAPI schema
#
# Triggers:
#   - Push to any branch
#   - Pull requests
#   - Manual workflow dispatch
#
# Matrix:
#   - Python versions: 3.9, 3.10, 3.11, 3.12, 3.13
#
# ref: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python
#
# ==============================================================================

name: mcp_client Python package

on: [push, pull_request, workflow_dispatch]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v5
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r test-requirements.txt
      - name: Test with pytest
        run: |
          pytest --cov=mcp_client
