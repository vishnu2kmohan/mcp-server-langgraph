# GCS Backend Configuration for WIF-Only Environment
# Copy this file to backend-config.tfbackend and update with your values
# IMPORTANT: backend-config.tfbackend is git-ignored for security

# GCS bucket for terraform state
# This bucket must be created manually before running terraform init
bucket = "YOUR-TERRAFORM-STATE-BUCKET-NAME"

# Path prefix within the bucket for this environment's state
prefix = "environments/gcp-staging-wif-only"

# GCP project where the state bucket resides (usually same as deployment project)
# Optional - defaults to the provider project if not specified
# project = "YOUR-GCP-PROJECT-ID"

# Enable state locking and consistency checks
# Optional - defaults to true
# enable_bucket_policy_only = true

# Usage:
# 1. Copy this file: cp backend-config.tfbackend.example backend-config.tfbackend
# 2. Update the bucket name and other values
# 3. Create the GCS bucket if it doesn't exist:
#    gcloud storage buckets create gs://YOUR-TERRAFORM-STATE-BUCKET-NAME \
#      --project=YOUR-GCP-PROJECT-ID \
#      --location=us-central1 \
#      --uniform-bucket-level-access
# 4. Initialize terraform:
#    terraform init -backend-config=backend-config.tfbackend
