# This is a template - DO NOT commit real secrets to git
# Use External Secrets Operator, Sealed Secrets, or cloud-native secret management
apiVersion: v1
kind: Secret
metadata:
  name: langgraph-agent-secrets
  namespace: langgraph-agent
  labels:
    app: langgraph-agent
type: Opaque
stringData:
  # These should be base64 encoded in production
  # Use: echo -n "your-secret" | base64
  anthropic-api-key: "REPLACE_WITH_ACTUAL_KEY"
  jwt-secret-key: "REPLACE_WITH_ACTUAL_SECRET"
  openfga-store-id: "REPLACE_WITH_STORE_ID"
  openfga-model-id: "REPLACE_WITH_MODEL_ID"
  infisical-client-id: ""
  infisical-client-secret: ""
  infisical-project-id: ""
