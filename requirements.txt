# ==============================================================================
# DEPRECATED: This file is maintained for backward compatibility only
# ==============================================================================
#
# This project uses uv for dependency management. Please use:
#   uv sync                    # Install all dependencies
#   uv sync --extra secrets    # Install with optional [secrets] dependencies
#   uv sync --extra embeddings # Install with optional [embeddings] dependencies
#
# All dependencies are defined in pyproject.toml
# This file may be removed in a future release
#
# Migration guide: https://github.com/vishnu2kmohan/mcp-server-langgraph/blob/main/docs/guides/uv-migration.md
# ==============================================================================

# LangGraph and LangChain
langgraph>=1.0.1
langgraph-checkpoint-redis>=0.1.2  # Redis checkpointer for distributed conversation state
langchain-core>=1.0.0

# LangSmith for observability
langsmith>=0.4.37

# LangGraph CLI for platform deployment
langgraph-cli>=0.1.0

# LiteLLM for multi-provider LLM support
litellm>=1.78.5

# MCP Server
mcp>=1.18.0

# Authentication
PyJWT>=2.10.1
cryptography>=46.0.3
bcrypt>=5.0.0  # Password hashing for InMemoryUserProvider
python-keycloak>=5.8.1
authlib>=1.6.5

# OpenTelemetry Core
# Aligned with pyproject.toml requirements (>=1.38.0)
# OpenTelemetry exporters require SDK version match (~=)
opentelemetry-api>=1.38.0
opentelemetry-sdk>=1.38.0

# OpenTelemetry Instrumentation
# Must match SDK version (0.59b0 for SDK 1.38.0)
opentelemetry-instrumentation-logging>=0.59b0

# OpenTelemetry Exporters
opentelemetry-exporter-otlp-proto-grpc>=1.38.0
opentelemetry-exporter-otlp-proto-http>=1.38.0

# Fine-grained Authorization
openfga-sdk>=0.9.7

# Secrets Management
infisical-python>=2.1.0

# Utilities
python-dotenv>=1.1.1
pydantic>=2.12.3
httpx>=0.28.1
pyyaml>=6.0.3

# Health checks and API
fastapi>=0.119.1
uvicorn[standard]>=0.38.0
pydantic-settings>=2.11.0

# Session management and caching
redis[hiredis]>=6.4.0  # Redis for sessions and conversation checkpoints

# Scheduling and background tasks
apscheduler>=3.11.0

# Logging
python-json-logger>=4.0.0

# Embeddings (Anthropic Best Practices - Just-in-Time Context Loading)
qdrant-client>=1.15.1
langchain-google-genai>=3.0.0
