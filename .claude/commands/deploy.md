---
description: Deploy mcp-server-langgraph to Kubernetes, GKE, Cloud Run, Helm, or Kustomize targets. Use when deploying the application to any environment.
allowed-tools:
  - Bash(docker build:*)
  - Bash(docker push:*)
  - Bash(kubectl get:*)
  - Bash(kubectl describe:*)
  - Bash(kubectl logs:*)
  - Bash(helm list:*)
  - Bash(helm status:*)
  - Bash(gcloud run services list:*)
  - Bash(gcloud run services describe:*)
  - Bash(gcloud container clusters list:*)
  - Bash(gcloud container clusters describe:*)
  - Bash(make:*)
  - Read
  - Glob
  - Grep
disable-model-invocation: true
context: fork
---
See skill: `.claude/skills/deploy/SKILL.md`

This command has been migrated to a skill for progressive disclosure.
Use `/deploy` to invoke, or read the skill directly at `.claude/skills/deploy/SKILL.md`.
