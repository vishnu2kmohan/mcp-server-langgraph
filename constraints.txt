# Dependency Constraints for mcp-server-langgraph
# ================================================
#
# This file provides upper bounds for volatile dependencies to prevent
# breaking changes from affecting downstream consumers.
#
# Usage (for template consumers):
#   pip install -c constraints.txt mcp-server-langgraph
#
# These constraints are ADVISORY for consumers, not enforced by the package.
# The package uses uv.lock for reproducible builds.
#
# Last updated: 2025-12-17

# LangChain Ecosystem (frequent breaking changes)
langchain-core>=1.1.0,<2.0.0
langgraph>=1.0.4,<2.0.0
langgraph-checkpoint-redis>=0.2.1,<1.0.0
langsmith>=0.4.49,<1.0.0

# LiteLLM (rapid iteration, API changes)
litellm>=1.80.5,<2.0.0

# MCP (Model Context Protocol - evolving specification)
mcp>=1.23.3,<2.0.0

# LLM Provider SDKs (API versioning)
anthropic>=0.46.0,<1.0.0
openai>=1.82.0,<3.0.0  # 2.x required by litellm

# Pydantic Ecosystem (major version = breaking)
pydantic>=2.12.5,<3.0.0
pydantic-settings>=2.12.0,<3.0.0
pydantic-ai>=1.25.0,<2.0.0

# OpenTelemetry (beta modules)
opentelemetry-api>=1.38.0,<2.0.0
opentelemetry-sdk>=1.38.0,<2.0.0
opentelemetry-instrumentation-logging>=0.59b0,<1.0.0
opentelemetry-instrumentation-fastapi>=0.59b0,<1.0.0
opentelemetry-instrumentation-httpx>=0.59b0,<1.0.0

# Web Framework
fastapi>=0.122.0,<1.0.0
starlette>=0.50.0,<1.0.0

# Database
sqlalchemy>=2.0.44,<3.0.0
asyncpg>=0.29.0,<1.0.0
redis>=6.4.0,<7.0.0

# Security
openfga-sdk>=0.9.8,<1.0.0
PyJWT>=2.10.1,<3.0.0
python-keycloak>=5.8.1,<6.0.0

# Google Cloud (major version = breaking)
google-cloud-aiplatform>=1.38.0,<2.0.0

# AWS SDK (use compatible release pattern)
boto3>=1.35.0,<2.0.0

# APScheduler (4.x has breaking API)
apscheduler>=3.11.0,<4.0.0
