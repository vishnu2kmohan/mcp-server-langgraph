================================================================================
Manual Python + UV Setup Migration Report
================================================================================

📊 Summary
  Workflows with manual setup: 2
  Total jobs to migrate: 5

🔄 Workflows to Migrate

  ci.yaml (4 jobs)
    • test (11 steps)
    • coverage-merge (8 steps)
    • pre-commit (8 steps)
    • push-stage-validators (7 steps)
  weekly-reports.yaml (1 jobs)
    • regenerate-reports (10 steps)

================================================================================
💡 Migration Instructions
================================================================================

Replace this manual sequence:
```yaml
- name: Set up Python
  uses: actions/setup-python@v6
  with:
    python-version: '3.12'

- name: Install uv
  uses: astral-sh/setup-uv@v5
  with:
    version: "latest"

- name: Install dependencies
  run: uv sync --frozen
```

With composite action:
```yaml
- name: Set up Python and dependencies
  uses: ./.github/actions/setup-python-deps
  with:
    python-version: '3.12'
    extras: ''  # Add extras if needed (e.g., 'dev builder')
```

================================================================================
Generated by: scripts/dev/find_manual_python_uv_setup.py
================================================================================
