---
# ServiceAccount for PostgreSQL
# Minimal permissions: Can read own secrets, access storage
apiVersion: v1
kind: ServiceAccount
metadata:
  name: postgres-sa
  namespace: mcp-server-langgraph
  labels:
    app: postgres
    component: database

---
# ServiceAccount for Redis
# Minimal permissions: Can read own secrets, access storage
apiVersion: v1
kind: ServiceAccount
metadata:
  name: redis-sa
  namespace: mcp-server-langgraph
  labels:
    app: redis
    component: cache

---
# ServiceAccount for Keycloak
# Minimal permissions: Can read own secrets, access database
apiVersion: v1
kind: ServiceAccount
metadata:
  name: keycloak-sa
  namespace: mcp-server-langgraph
  labels:
    app: keycloak
    component: authentication

---
# ServiceAccount for OpenFGA
# Minimal permissions: Can read own secrets, access database
apiVersion: v1
kind: ServiceAccount
metadata:
  name: openfga-sa
  namespace: mcp-server-langgraph
  labels:
    app: openfga
    component: authorization

---
# ServiceAccount for Qdrant
# Minimal permissions: Can read own secrets, access storage
apiVersion: v1
kind: ServiceAccount
metadata:
  name: qdrant-sa
  namespace: mcp-server-langgraph
  labels:
    app: qdrant
    component: vector-database
