apiVersion: v1
kind: Service
metadata:
  name: mcp-server-langgraph
  namespace: mcp-server-langgraph
  labels:
    app: mcp-server-langgraph
  annotations: {}
    # Cloud-specific annotations should be added via overlays:
    # AWS EKS: service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
    # GKE: cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  - name: sse
    port: 8001
    targetPort: http
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: http
    protocol: TCP
  selector:
    app: mcp-server-langgraph
---
# Headless service for SSE connections (stateful)
apiVersion: v1
kind: Service
metadata:
  name: mcp-server-langgraph-sse
  namespace: mcp-server-langgraph
  labels:
    app: mcp-server-langgraph
    service-type: sse
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 86400  # 24 hours for long-lived SSE connections
  ports:
  - name: sse
    port: 8000
    targetPort: http
    protocol: TCP
  selector:
    app: mcp-server-langgraph
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-server-langgraph-headless
  namespace: mcp-server-langgraph
  labels:
    app: mcp-server-langgraph
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  selector:
    app: mcp-server-langgraph
