apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-server-langgraph
  namespace: mcp-server-langgraph
  labels:
    app: mcp-server-langgraph
  # Cloud-specific annotations are provided by overlays
  # See:
  #   - deployments/kubernetes/overlays/aws/serviceaccount-patch.yaml (AWS EKS with IRSA)
  #   - deployments/overlays/production-gke/serviceaccount-patch.yaml (GKE with Workload Identity)
  #   - deployments/kubernetes/overlays/azure/kustomization.yaml (Azure AKS with Workload Identity)
