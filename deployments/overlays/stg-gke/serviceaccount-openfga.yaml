apiVersion: v1
kind: ServiceAccount
metadata:
  name: openfga-sa
  namespace: stg-mcp-server-langgraph
  annotations:
    # Workload Identity - minimal permissions for OpenFGA
    # This service account has permissions for:
    # - Cloud SQL (cloudsql.client) ONLY
    iam.gke.io/gcp-service-account: stg-openfga@vishnu-sandbox-20250310.iam.gserviceaccount.com
