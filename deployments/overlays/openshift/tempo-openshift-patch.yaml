# Tempo StatefulSet - OpenShift security context
# ===============================================
# Configure Tempo for OpenShift restricted-v2 SCC
# OpenShift assigns arbitrary UIDs via SCC (Security Context Constraints)
# All processes must belong to GID 0 (root group) for file access
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: tempo
spec:
  template:
    spec:
      securityContext:
        runAsUser: null
        runAsGroup: 0
        fsGroup: 0
      initContainers:
        - name: init-permissions
          securityContext:
            runAsUser: null
            runAsNonRoot: true
            runAsGroup: 0
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      containers:
        - name: tempo
          securityContext:
            runAsUser: null
            runAsGroup: 0
