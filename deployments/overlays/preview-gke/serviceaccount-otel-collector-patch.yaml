apiVersion: v1
kind: ServiceAccount
metadata:
  name: otel-collector
  annotations:
    # Workload Identity annotation for GCP service account binding
    # This allows the pod to authenticate as the GCP service account
    # and access Cloud Monitoring APIs
    iam.gke.io/gcp-service-account: preview-otel-collector@vishnu-sandbox-20250310.iam.gserviceaccount.com
