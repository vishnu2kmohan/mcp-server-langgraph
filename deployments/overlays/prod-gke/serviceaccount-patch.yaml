apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-server-langgraph
  annotations:
    # Workload Identity annotation
    # UPDATE THIS: Replace vishnu-prod-project with your actual GCP project ID
    # Format: {service-account}@{project-id}.iam.gserviceaccount.com
    iam.gke.io/gcp-service-account: mcp-prod-app-sa@vishnu-prod-project.iam.gserviceaccount.com
