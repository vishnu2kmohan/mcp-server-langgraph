apiVersion: v1
kind: ServiceAccount
metadata:
  name: keycloak-sa
  namespace: staging-mcp-server-langgraph
  annotations:
    # Workload Identity - minimal permissions for Keycloak
    # This service account has permissions for:
    # - Cloud SQL (cloudsql.client) ONLY
    iam.gke.io/gcp-service-account: staging-keycloak@vishnu-sandbox-20250310.iam.gserviceaccount.com
