apiVersion: v1
kind: ServiceAccount
metadata:
  name: openfga
  namespace: mcp-staging
  annotations:
    # Workload Identity - minimal permissions for OpenFGA
    # This service account has permissions for:
    # - Cloud SQL (cloudsql.client) ONLY
    iam.gke.io/gcp-service-account: staging-openfga@vishnu-sandbox-20250310.iam.gserviceaccount.com
