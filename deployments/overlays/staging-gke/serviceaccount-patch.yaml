apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-server-langgraph
  annotations:
    # Workload Identity - allows pods to authenticate as GCP service account
    # This service account has permissions for:
    # - Secret Manager (secretmanager.secretAccessor)
    # - Cloud SQL (cloudsql.client)
    # - Cloud Logging (logging.logWriter)
    # - Cloud Monitoring (monitoring.metricWriter)
    # - Vertex AI (aiplatform.user, aiplatform.developer) - added for Vertex AI access
    iam.gke.io/gcp-service-account: mcp-staging-sa@vishnu-sandbox-20250310.iam.gserviceaccount.com
