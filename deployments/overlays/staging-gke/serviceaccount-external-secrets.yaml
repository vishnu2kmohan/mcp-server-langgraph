apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-operator
  namespace: mcp-staging
  annotations:
    # Workload Identity - minimal permissions for External Secrets Operator
    # This service account has permissions for:
    # - Secret Manager (secretmanager.secretAccessor) ONLY
    iam.gke.io/gcp-service-account: external-secrets-staging@vishnu-sandbox-20250310.iam.gserviceaccount.com
