apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-collector
spec:
  template:
    spec:
      containers:
      - name: otel-collector
        imagePullPolicy: Always  # Security: Always pull latest image for staging environment
