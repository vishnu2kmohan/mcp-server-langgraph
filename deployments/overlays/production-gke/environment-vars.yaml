apiVersion: v1
kind: ConfigMap
metadata:
  name: environment-vars
  namespace: production-mcp-server-langgraph
data:
  # GCP Project ID - UPDATE THIS with your actual GCP project ID
  # This ConfigMap is used as a source for Kustomize variable substitution
  # For production deployments, update deployment-config ConfigMap instead
  GCP_PROJECT_ID: "vishnu-production-project"
