apiVersion: apps/v1
kind: Deployment
metadata:
  name: qdrant
spec:
  template:
    spec:
      # Pod-level security context
      securityContext:
        runAsNonRoot: true
        runAsUser: 10000
        runAsGroup: 10000
        fsGroup: 10000
        seccompProfile:
          type: RuntimeDefault

      containers:
      - name: qdrant
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
            ephemeral-storage: 1Gi
          limits:
            cpu: 1000m
            memory: 1Gi
            ephemeral-storage: 2Gi
        securityContext:
          runAsUser: 10000
          runAsGroup: 10000
          allowPrivilegeEscalation: false
          # Note: Qdrant requires writable filesystem for data storage
          readOnlyRootFilesystem: false
          capabilities:
            drop:
              - ALL

        # Make probes different (readiness more sensitive than liveness)
        readinessProbe:
          failureThreshold: 1  # Fail faster for readiness

        livenessProbe:
          periodSeconds: 30  # Check less frequently
          timeoutSeconds: 10  # Longer timeout
          failureThreshold: 5  # More tolerant before restart
