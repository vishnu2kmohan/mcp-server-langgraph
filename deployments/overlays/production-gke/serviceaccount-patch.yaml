apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-server-langgraph
  annotations:
    # Workload Identity annotation
    # Replace my-gcp-project with your actual project ID
    iam.gke.io/gcp-service-account: mcp-prod-app-sa@my-gcp-project.iam.gserviceaccount.com
