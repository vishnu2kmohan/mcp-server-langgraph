apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-server-langgraph
  annotations:
    # Workload Identity annotation
    # NOTE: For production deployments, use Helm chart (values-production-gke.yaml)
    # If using Kustomize, replace PLACEHOLDER_GCP_PROJECT_ID before deploying:
    # kustomize build . | sed 's/PLACEHOLDER_GCP_PROJECT_ID/my-project-id/g' | kubectl apply -f -
    iam.gke.io/gcp-service-account: mcp-prod-app-sa@PLACEHOLDER_GCP_PROJECT_ID.iam.gserviceaccount.com
