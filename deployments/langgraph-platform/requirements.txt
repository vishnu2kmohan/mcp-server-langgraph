# LangGraph Platform deployment requirements
# Lighter dependency set for platform deployment

# Core LangGraph and LangChain
langgraph>=0.2.28
langchain-core>=0.3.15
langsmith>=0.1.0

# Multi-LLM support
litellm>=1.78.0

# Authentication
PyJWT>=2.8.0

# Utilities
python-dotenv>=1.0.1
pydantic>=2.5.3
pydantic-settings>=2.1.0
httpx>=0.26.0

# Optional: Fine-grained authorization (if using OpenFGA on platform)
openfga-sdk>=0.5.1

# Optional: Secrets management (if using Infisical)
infisical-python>=2.1.7
