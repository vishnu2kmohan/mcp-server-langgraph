# LangGraph Platform Deployment Requirements
# ============================================
#
# This is a SPECIALIZED requirements file for LangGraph Platform deployment.
# It contains a lighter dependency set optimized for the managed platform.
#
# Note: The main project uses uv/pyproject.toml. This file is maintained
# separately for LangGraph Platform compatibility and has a reduced dependency set.
#
# Sync with: src/mcp_server_langgraph when updating versions
# ============================================

# Core LangGraph and LangChain
langgraph>=0.2.28
langchain-core>=0.3.15
langsmith>=0.1.0

# Multi-LLM support
litellm>=1.78.0

# Authentication
PyJWT>=2.8.0

# Utilities
python-dotenv>=1.0.1
pydantic>=2.5.3
pydantic-settings>=2.1.0
httpx>=0.26.0

# Optional: Fine-grained authorization (if using OpenFGA on platform)
openfga-sdk>=0.5.1

# Optional: Secrets management (if using Infisical)
infisical-python>=2.1.7
