# LangGraph Platform Deployment Requirements
# ============================================
#
# EXCEPTION: This requirements.txt file exists for LangGraph Platform compatibility.
#
# The main project uses uv/pyproject.toml as the source of truth.
# This file contains a lighter dependency set optimized for the managed platform.
#
# DO NOT use this file for local development - use `uv sync` instead.
#
# Source of truth: ../../pyproject.toml
# Sync with: src/mcp_server_langgraph when updating versions
# Why this exists: LangGraph Platform requires requirements.txt format
#
# ============================================

# Core LangGraph and LangChain
langgraph>=0.2.28
langchain-core>=0.3.15
langsmith>=0.1.0

# Multi-LLM support
litellm>=1.78.0

# Authentication
PyJWT>=2.8.0

# Utilities
python-dotenv>=1.0.1
pydantic>=2.5.3
pydantic-settings>=2.1.0
httpx>=0.26.0

# Optional: Fine-grained authorization (if using OpenFGA on platform)
openfga-sdk>=0.5.1

# Optional: Secrets management (if using Infisical)
infisical-python>=2.1.7
