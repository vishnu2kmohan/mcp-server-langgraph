---
title: 'MCP Server Guide'
description: 'Build and deploy MCP servers for tool and resource sharing across AI applications'
icon: 'server'
seoTitle: 'MCP Server Development Guide - MCP Server LangGraph'
seoDescription: 'Complete guide to building MCP servers. Learn server architecture, tool registration, resource management, and deployment patterns.'
keywords:
  - MCP server
  - Model Context Protocol
  - server development
  - tool registration
  - resources
  - prompts
contentType: 'how-to'
---

# MCP Server Guide

Build MCP servers that expose tools, resources, and prompts to AI applications.

## Overview

MCP (Model Context Protocol) servers provide:

| Capability | Description |
|------------|-------------|
| **Tools** | Functions the AI can call |
| **Resources** | Data sources the AI can read |
| **Prompts** | Reusable prompt templates |
| **Sampling** | Request LLM completions |

---

## Quick Start

### Basic Server

```python
from mcp.server import Server
from mcp.types import Tool, TextContent

app = Server("my-mcp-server")

@app.list_tools()
async def list_tools() -> list[Tool]:
    return [
        Tool(
            name="hello",
            description="Say hello",
            inputSchema={
                "type": "object",
                "properties": {
                    "name": {"type": "string"}
                },
                "required": ["name"]
            }
        )
    ]

@app.call_tool()
async def call_tool(name: str, arguments: dict) -> list[TextContent]:
    if name == "hello":
        return [TextContent(type="text", text=f"Hello, {arguments['name']}!")]
    raise ValueError(f"Unknown tool: {name}")

if __name__ == "__main__":
    import asyncio
    from mcp.server.stdio import stdio_server

    asyncio.run(stdio_server(app))
```

### Running the Server

```bash
# Stdio transport (for Claude Desktop)
python my_server.py

# SSE transport (for web clients)
uvicorn my_server:app --port 8000
```

---

## Server Architecture

```
┌─────────────────────────────────────────┐
│              MCP Server                  │
├─────────────────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│  │  Tools  │ │Resources│ │ Prompts │   │
│  └────┬────┘ └────┬────┘ └────┬────┘   │
│       │           │           │         │
│  ┌────┴───────────┴───────────┴────┐   │
│  │        Request Handler           │   │
│  └──────────────┬──────────────────┘   │
│                 │                       │
│  ┌──────────────┴──────────────────┐   │
│  │         Transport Layer          │   │
│  │   (stdio / SSE / WebSocket)      │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## Resources

Expose data sources that AI can read:

```python
from mcp.types import Resource, TextResourceContents

@app.list_resources()
async def list_resources() -> list[Resource]:
    return [
        Resource(
            uri="file:///config.json",
            name="Configuration",
            description="Application configuration"
        )
    ]

@app.read_resource()
async def read_resource(uri: str) -> TextResourceContents:
    if uri == "file:///config.json":
        config = load_config()
        return TextResourceContents(
            uri=uri,
            mimeType="application/json",
            text=json.dumps(config)
        )
```

---

## Prompts

Define reusable prompt templates:

```python
from mcp.types import Prompt, PromptMessage

@app.list_prompts()
async def list_prompts() -> list[Prompt]:
    return [
        Prompt(
            name="summarize",
            description="Summarize text",
            arguments=[
                {"name": "text", "description": "Text to summarize", "required": True}
            ]
        )
    ]

@app.get_prompt()
async def get_prompt(name: str, arguments: dict) -> list[PromptMessage]:
    if name == "summarize":
        return [
            PromptMessage(
                role="user",
                content=f"Please summarize this text:\n\n{arguments['text']}"
            )
        ]
```

---

## Deployment

### Docker

```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY . .
RUN pip install -e .
CMD ["python", "-m", "my_mcp_server"]
```

### Kubernetes

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: mcp-server
          image: my-mcp-server:latest
          ports:
            - containerPort: 8000
```

---

## Related Documentation

- [MCP Protocol Integration](/integrations/mcp-protocol)
- [Client Integration Guide](/guides/client-integration)
- [Tool Development Guide](/guides/tool-development)
- [ADR-0082: MCP Client Capabilities](/architecture/adr-0082-mcp-client-capabilities)
